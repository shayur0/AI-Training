
# Base URL of the deployed backend, no trailing slash (e.g. https://illumination-space-backend.onrender.com).
# Leave unset for local dev — Vite's dev server proxies /api to localhost:3001 instead (see vite.config.js).
VITE_API_BASE_URL=
